---
// /en/identify → redirected to /en/observe (consolidated — see issue #273)
// Keeps the identify concept but within the unified observe flow.
// Permanent redirect so old links and search engines update.
return Astro.redirect('/en/observe/?mode=identify', 301);
---
